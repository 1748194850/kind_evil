# 阶段零：场景配置指南 - Unity编辑器操作

> **目标：** 在Unity编辑器中配置基础场景，无需修改代码  
> **预计时间：** 30-60分钟

---

## 📋 任务清单

- [ ] 步骤1：配置GameInitializer（必须！）
- [ ] 步骤2：配置SceneBounds
- [ ] 步骤3：创建地面和场景元素
- [ ] 步骤4：配置Player使用新组件系统
- [ ] 步骤5：测试场景

---

## 步骤1：配置GameInitializer（最重要！）

### 1.1 打开测试场景

1. 在Unity编辑器中，打开场景：
   - `Assets/Scenes/1_Gameplay/test_Scence/test_sence.scene`
   - 或创建新场景

### 1.2 创建Managers GameObject

1. 在Hierarchy中右键点击 → `Create Empty`
2. 命名为 `Managers`
3. 设置Position为 `(0, 0, 0)`

### 1.3 添加GameInitializer

1. 在Hierarchy中右键点击 `Managers` → `Create Empty`
2. 命名为 `GameInitializer`
3. 在Inspector中点击 `Add Component`
4. 搜索并添加 `GameInitializer` 脚本
5. 在Inspector中检查：
   - ✅ **Register On Awake**: 勾选（默认已勾选）
   - ✅ **Show Debug Logs**: 勾选（方便调试）

### 1.4 设置执行顺序（重要！）

1. 点击菜单栏 `Edit` → `Project Settings`
2. 在左侧选择 `Script Execution Order`
3. 点击 `+` 按钮添加脚本
4. 选择 `GameInitializer`
5. 设置值为 `-100`（确保最早执行）
6. 点击 `Apply`

**为什么重要？** GameInitializer必须在所有其他管理器之前初始化，否则依赖注入会失败。

---

## 步骤2：添加其他管理器

### 2.1 添加核心管理器

在 `Managers` GameObject下创建空对象，添加对应脚本：

| 管理器名称 | 操作步骤 |
|-----------|---------|
| `InputManager` | 创建空对象 → 添加 `InputManager` 组件 |
| `CameraManager` | 创建空对象 → 添加 `CameraManager` 组件 |
| `EventManager` | 创建空对象 → 添加 `EventManager` 组件 |
| `GameManager` | 创建空对象 → 添加 `GameManager` 组件 |
| `SceneManager` | 创建空对象 → 添加 `SceneManager` 组件 |
| `TimeManager` | 创建空对象 → 添加 `TimeManager` 组件 |
| `SaveManager` | 创建空对象 → 添加 `SaveManager` 组件 |
| `PoolManager` | 创建空对象 → 添加 `PoolManager` 组件 |

**⚠️ 重要：** 这些管理器使用单例模式，但**不会自动创建**——必须手动添加到场景中。如果场景中缺少某个管理器，运行时会在 Console 中看到 `[Singleton] XXX 实例未找到！` 的错误。请确保所有需要的管理器都已手动添加。

---

## 步骤3：配置SceneBounds

### 3.1 创建SceneBounds GameObject

1. 在Hierarchy中右键点击 → `Create Empty`
2. 命名为 `SceneBounds`
3. 设置Position为 `(0, 0, 0)`

### 3.2 添加SceneBounds组件

1. 选择 `SceneBounds` GameObject
2. 在Inspector中点击 `Add Component`
3. 搜索并添加 `SceneBounds` 脚本

### 3.3 配置边界参数

在Inspector的 `SceneBounds` 组件中设置：

**边界设置：**
- **Left Bound**: `-20`（左边界）
- **Right Bound**: `20`（右边界）
- **Bottom Bound**: `-5`（下边界）
- **Top Bound**: `10`（上边界）

**边界处理方式：**
- **Horizontal Mode**: `Block`（水平方向阻止移动）
- **Vertical Mode**: `Block`（垂直方向阻止移动）

**调试：**
- ✅ **Show Bounds In Editor**: 勾选（在Scene视图中显示边界线）
- **Bounds Color**: 黄色（边界线颜色）

### 3.4 验证边界显示

1. 在Scene视图中选择 `SceneBounds` GameObject
2. 你应该能看到黄色的边界线框
3. 如果看不到，检查 `Show Bounds In Editor` 是否勾选

---

## 步骤4：创建地面和场景元素

### 4.1 创建地面（Ground）

1. 在Hierarchy中右键点击 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Ground`
3. 设置Transform：
   - **Position**: `(0, -3, 0)`
   - **Scale**: `(20, 1, 1)`（宽20，高1）
4. 在Inspector中：
   - **Sprite Renderer** → **Color**: 深灰色（例如：`#808080`）
   - **Sorting Layer**: `Default`（保持默认，不要修改！）
   - **Order in Layer**: `0`（保持默认，或设置为0）
5. 添加碰撞体：
   - 点击 `Add Component` → `Physics 2D` → `Box Collider 2D`
   - 确保 `Box Collider 2D` 的Size匹配Sprite大小

**重要提示：**
- Ground的Sorting Layer必须是 `Default`
- Order in Layer应该是 `0`（或正数，确保在Background前面）

### 4.2 创建背景（可选）

1. 在Hierarchy中右键点击 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Background`
3. 设置Transform：
   - **Position**: `(0, 0, 1)`（Z轴靠后）
   - **Scale**: `(30, 15, 1)`（更大的背景）
4. 在Inspector中：
   - **Sprite Renderer** → **Color**: 浅蓝色（例如：`#87CEEB`）
   - **Sorting Layer**: 保持 `Default`（不要创建新的Sorting Layer！）
   - **Order in Layer**: `-10`（确保在最后面，在Ground后面）

**重要提示：**
- **不要创建新的Sorting Layer**，使用默认的 `Default` Sorting Layer
- 只用 `Order in Layer` 来控制渲染顺序
- Order in Layer 数值越小，越靠后（越先渲染，越在底层）
- Order in Layer 数值越大，越靠前（越晚渲染，越在上层）

---

## 步骤5：配置Player GameObject

### 5.1 检查Player是否存在

如果场景中已有Player GameObject，继续下一步。  
如果没有，创建Player：

1. 在Hierarchy中右键点击 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Player`
3. 设置Transform：
   - **Position**: `(0, 0, 0)`（玩家出生点）
   - **Scale**: `(1, 1, 1)`
4. 在Inspector中：
   - **Sprite Renderer** → **Color**: 红色（例如：`#FF0000`）
   - **Sorting Layer**: `Default`（保持默认）
   - **Order in Layer**: `0`（或更高，确保在Ground前面）

### 5.2 添加物理组件

1. 选择 `Player` GameObject
2. 添加 `Rigidbody2D` 组件：
   - 点击 `Add Component` → `Physics 2D` → `Rigidbody2D`
   - 设置 **Gravity Scale**: `3`（重力）
   - 设置 **Freeze Rotation Z**: ✅ 勾选（防止旋转）
3. 添加碰撞体：
   - 点击 `Add Component` → `Physics 2D` → `Box Collider 2D`

### 5.3 移除旧的PlayerController（如果存在）

1. 选择 `Player` GameObject
2. 在Inspector中找到 `PlayerController` 组件
3. 点击组件右上角的 `⋮` → `Remove Component`
4. **或者保留它**（如果暂时还需要）

### 5.4 添加新的玩家组件

在 `Player` GameObject上添加以下组件：

#### 5.4.1 PlayerMovement（移动控制）

1. 点击 `Add Component`
2. 搜索 `PlayerMovement`
3. 添加组件
4. 在Inspector中设置：
   - **Move Speed**: `8`（移动速度）

#### 5.4.2 GroundChecker（地面检测）

1. 点击 `Add Component`
2. 搜索 `GroundChecker`
3. 添加组件
4. 在Inspector中设置：
   - **Ground Check Distance**: `0.3`
   - **Ground Layer**: 选择 `Ground` 层（⚠️ **禁止**设为 `Nothing` 或 `Everything`，设为 Nothing 会触发 LogError 并禁用地面检测，设为 Everything 会触发 LogWarning）
   - **Max Ground Angle**: `60`（默认值，超过此角度的表面视为墙壁而非地面）
   - **Show Debug Info**: 取消勾选（默认关闭）

> **前置条件：** 确保 Unity 的 `Tags and Layers` 中已创建 `Ground` 层，并且地面 GameObject 的 Layer 已设为 `Ground`。

#### 5.4.3 PlayerJump（跳跃控制）

1. 点击 `Add Component`
2. 搜索 `PlayerJump`
3. 添加组件
4. 在Inspector中设置：
   - **Jump Force**: `8`
   - **Require Grounded**: ✅ 勾选
   - **Allow Air Jump**: 取消勾选

#### 5.4.4 PlayerSpriteFlipper（精灵翻转）

1. 点击 `Add Component`
2. 搜索 `PlayerSpriteFlipper`
3. 添加组件（无需额外配置）

#### 5.4.5 PlayerBoundaryChecker（边界检测）

1. 点击 `Add Component`
2. 搜索 `PlayerBoundaryChecker`
3. 添加组件
4. 在Inspector中设置：
   - **Scene Bounds**: 将Hierarchy中的 `SceneBounds` GameObject拖拽到这里
   - **Check Boundary Every Frame**: ✅ 勾选

---

## 步骤6：配置摄像机

### 6.1 设置主摄像机

1. 在Hierarchy中选择 `Main Camera`
2. 在Inspector中设置：
   - **Projection**: `Orthographic`（2D游戏）
   - **Size**: `5`
   - **Position**: `(0, 0, -10)`
   - **Background**: 选择一个颜色（例如：浅蓝色）

### 6.2 添加CameraManager组件（如果还没有）

1. 选择 `Main Camera` GameObject
2. 点击 `Add Component`
3. 搜索 `CameraManager`
4. 添加组件
5. 在Inspector中配置：
   - **Default Exploration Config**: 可以留空（稍后配置）
   - **Show Debug Logs**: ✅ 勾选

**注意：** 如果CameraManager已经在Managers下，可以跳过这一步。

---

## 步骤7：测试场景

### 7.1 运行测试

1. 点击Unity编辑器顶部的 **Play** 按钮（▶️）
2. 观察Console窗口，应该看到：
   - `GameInitializer: IInputManager registered`
   - `GameInitializer: ICameraManager registered`
   - `GameInitializer: IEventManager registered`
   - `GameInitializer: All core services registered successfully!`

### 7.2 测试玩家功能

1. **移动测试**：
   - 按 `A` 或 `←` 键：玩家应该向左移动
   - 按 `D` 或 `→` 键：玩家应该向右移动

2. **跳跃测试**：
   - 按 `Space` 键：玩家应该跳跃
   - 确保玩家能正常落地

3. **边界测试**：
   - 移动到场景左边界：玩家应该被阻止，无法继续向左
   - 移动到场景右边界：玩家应该被阻止，无法继续向右

### 7.3 检查错误

如果Console中出现错误：

**错误："ServiceLocator.Instance is null"**
- 解决：确保GameInitializer在场景中，并且执行顺序设置为-100

**错误："[Singleton] XXX 实例未找到！"**
- 解决：对应的管理器 GameObject 未添加到场景中。**Singleton 不会自动创建**，必须在 Managers 下手动创建对应的 GameObject 并添加脚本组件

**错误："IInputManager not registered"**
- 解决：确保GameInitializer的 `Register On Awake` 已勾选
- 解决：确保InputManager已**手动添加**到场景中

**错误："[GroundChecker] XXX: Ground Layer 设为 Nothing！"**
- 解决：在Player的 GroundChecker 组件中，将 Ground Layer 从 Nothing 改为 `Ground` 层
- 前提：需要先在 `Tags and Layers` 中创建 `Ground` 层，并将地面的 Layer 设为 `Ground`

**警告："[GroundChecker] Ground Layer 设为 Everything"**
- 解决：建议改为只选择 `Ground` 层，避免在伤害区域等非地面物体上触发地面检测

**错误："PlayerMovement: ServiceLocator not found"**
- 解决：确保GameInitializer执行顺序为-100

---

## ✅ 完成检查清单

完成所有步骤后，检查：

- [ ] GameInitializer在场景中，执行顺序为-100
- [ ] 所有管理器都已**手动添加**到 Managers GameObject下（9个管理器：GameInitializer + 8个核心管理器）
- [ ] SceneBounds已配置，边界线在Scene视图中可见
- [ ] Ground已创建，有碰撞体，Layer 设为 `Ground`
- [ ] Player已配置所有新组件（GroundChecker 的 Ground Layer 设为 `Ground`）
- [ ] 运行游戏，Console无错误（注意检查 Singleton 和 GroundChecker 相关的 LogError）
- [ ] 玩家可以移动和跳跃
- [ ] 玩家不会掉出场景边界

---

**阶段零已完成！进入阶段一：战斗基础系统。**
