# 🚀 快速开始指南 - 5分钟创建你的第一个场景

## 步骤 1: 创建新场景 (30秒)
1. `File` → `New Scene` → 选择 `Basic (Built-in)`
2. `File` → `Save` → 保存到 `Assets/Scenes/1_Gameplay/TestScene`

## 步骤 2: 创建管理器 (1分钟)
1. 在 Hierarchy 中右键 → `Create Empty` → 命名为 `Managers`
2. 在 `Managers` 下创建空对象，添加以下脚本：
   - `GameManager` (添加 `GameManager` 组件)
   - `InputManager` (添加 `InputManager` 组件)
   - `EventManager` (添加 `EventManager` 组件)

## 步骤 3: 创建玩家对象 (1分钟)
1. `Hierarchy` → 右键 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Player`
3. 设置颜色为红色
4. 添加 `Rigidbody2D` 组件
5. 添加 `Box Collider 2D` 组件
6. 添加 `PlayerController` 脚本（从 `Assets/Scenes/1_Gameplay/PlayerController.cs`）

## 步骤 4: 创建地面 (30秒)
1. `Hierarchy` → 右键 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Ground`
3. 设置 Position: `(0, -3, 0)`
4. 设置 Scale: `(10, 1, 1)`
5. 添加 `Box Collider 2D` 组件

## 步骤 5: 配置输入 (1分钟)
1. `Edit` → `Project Settings` → `Input Manager`
2. 确保以下输入轴存在：
   - **Horizontal**: A/D 或 ←/→
   - **Jump**: Space
   - **Fire1**: Left Ctrl

## 步骤 6: 运行测试 (30秒)
1. 点击 **Play** 按钮 (▶️)
2. 使用 `A/D` 键移动
3. 使用 `Space` 键跳跃

## ✅ 完成！

如果一切正常，你应该能看到：
- 玩家对象在场景中
- 可以左右移动
- 可以跳跃
- Console 中显示 "GameManager Initialized"

---

## 🐛 遇到问题？

### 玩家不移动？
- 检查 `PlayerController` 脚本是否添加到 Player 对象
- 检查 Input Manager 中的输入轴配置
- 查看 Console 是否有错误信息

### 玩家掉出场景？
- 确保 Ground 对象有碰撞体
- 检查 Ground 的 Position 是否正确

### 管理器报错？
- 确保所有管理器脚本都在 `Assets/Scenes/0_Core/` 目录下
- 检查命名空间是否正确

---

## 📚 详细教程

查看 [创建第一个场景](创建第一个场景.md) 获取完整详细教程。

---

## 📁 更多文档

所有中文文档都在 `Assets/中文文档/` 文件夹中：
- **教程/** - 学习教程
- **问题排查/** - 问题排查指南

