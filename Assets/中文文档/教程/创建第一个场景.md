# Unity 场景创建教程 - 创建你的第一个场景

## 📋 目录
1. [创建新场景](#1-创建新场景)
2. [设置场景基础](#2-设置场景基础)
3. [添加核心管理器](#3-添加核心管理器)
4. [创建测试对象](#4-创建测试对象)
5. [配置输入系统](#5-配置输入系统)
6. [测试场景](#6-测试场景)

---

## 1. 创建新场景

### 步骤：
1. 在 Unity 编辑器中，点击菜单栏 `File` → `New Scene`
2. 选择 `Basic (Built-in)` 模板
3. 点击 `Create` 创建场景
4. 保存场景：
   - 按 `Ctrl + S` 或点击 `File` → `Save`
   - 保存到 `Assets/Scenes/1_Gameplay/` 文件夹
   - 命名为 `TestScene` 或 `FirstScene`

---

## 2. 设置场景基础

### 2.1 设置摄像机
1. 在 Hierarchy 中选择 `Main Camera`
2. 在 Inspector 中设置：
   - **Projection**: `Orthographic` (2D游戏) 或 `Perspective` (3D游戏)
   - **Size**: `5` (2D) 或 **Field of View**: `60` (3D)
   - **Position**: `(0, 0, -10)` (2D) 或 `(0, 1, -10)` (3D)

### 2.2 设置场景背景
1. 选择 `Main Camera`
2. 在 Inspector 的 Camera 组件中：
   - **Background**: 选择一个颜色（例如：浅蓝色 `#87CEEB`）

---

## 3. 添加核心管理器

### 3.1 创建管理器父对象
1. 在 Hierarchy 中右键点击 → `Create Empty`
2. 命名为 `Managers`
3. 设置 Position 为 `(0, 0, 0)`

### 3.2 添加 GameManager
1. 在 Hierarchy 中右键点击 `Managers` → `Create Empty`
2. 命名为 `GameManager`
3. 在 Inspector 中点击 `Add Component`
4. 搜索并添加 `GameManager` 脚本
5. 在 Inspector 中设置：
   - **Current Game State**: `MainMenu` 或 `Playing`

### 3.3 添加其他核心管理器
按照相同方式添加以下管理器（都是空对象，添加对应脚本）：

| 管理器名称 | 脚本组件 |
|-----------|---------|
| `EventManager` | `EventManager` |
| `InputManager` | `InputManager` |
| `TimeManager` | `TimeManager` |
| `SceneManager` | `SceneManager` |
| `SaveManager` | `SaveManager` |
| `PoolManager` | `PoolManager` |

**提示**：所有管理器都会自动创建（因为使用了 Singleton 模式），但手动添加可以让它们在 Hierarchy 中可见，方便调试。

---

## 4. 创建测试对象

### 4.1 创建一个简单的玩家对象（2D示例）

#### 步骤：
1. 在 Hierarchy 中右键点击 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Player`
3. 设置 Transform：
   - **Position**: `(0, 0, 0)`
   - **Scale**: `(1, 1, 1)`
4. 在 Inspector 中：
   - 选择 Sprite Renderer 组件
   - 点击 **Color** 选择一个颜色（例如：红色）
5. 添加碰撞体：
   - 点击 `Add Component` → `Physics 2D` → `Box Collider 2D`

### 4.2 创建地面

#### 步骤：
1. 在 Hierarchy 中右键点击 → `2D Object` → `Sprite` → `Square`
2. 命名为 `Ground`
3. 设置 Transform：
   - **Position**: `(0, -3, 0)`
   - **Scale**: `(10, 1, 1)` （更宽的地面）
4. 在 Inspector 中：
   - Sprite Renderer → **Color**: 深灰色
5. 添加碰撞体：
   - `Add Component` → `Physics 2D` → `Box Collider 2D`

### 4.3 创建一个简单的玩家控制器脚本

创建一个新脚本 `PlayerController.cs`：

```csharp
using UnityEngine;
using Core.Managers;

namespace Gameplay
{
    public class PlayerController : MonoBehaviour
    {
        [Header("移动设置")]
        [SerializeField] private float moveSpeed = 5f;
        [SerializeField] private float jumpForce = 10f;
        
        private Rigidbody2D rb;
        private bool isGrounded = false;
        
        private void Start()
        {
            rb = GetComponent<Rigidbody2D>();
            if (rb == null)
            {
                rb = gameObject.AddComponent<Rigidbody2D>();
            }
            
            // 订阅输入事件
            InputManager.Instance.OnJumpPressed += Jump;
        }
        
        private void Update()
        {
            // 获取移动输入
            Vector2 moveInput = InputManager.Instance.GetMoveInput();
            
            // 移动
            rb.velocity = new Vector2(moveInput.x * moveSpeed, rb.velocity.y);
        }
        
        private void Jump()
        {
            if (isGrounded)
            {
                rb.AddForce(Vector2.up * jumpForce, ForceMode2D.Impulse);
            }
        }
        
        private void OnDestroy()
        {
            // 取消订阅
            if (InputManager.Instance != null)
            {
                InputManager.Instance.OnJumpPressed -= Jump;
            }
        }
    }
}
```

**保存脚本后**：
1. 将脚本添加到 `Player` 对象上
2. 在 Inspector 中设置参数：
   - **Move Speed**: `5`
   - **Jump Force**: `10`

---

## 5. 配置输入系统

### 5.1 设置 Unity 输入管理器

1. 点击菜单栏 `Edit` → `Project Settings`
2. 选择 `Input Manager`
3. 确保以下输入轴存在：

#### Horizontal（水平移动）
- **Positive Button**: `d` 或 `right arrow`
- **Negative Button**: `a` 或 `left arrow`
- **Alt Positive Button**: `right`
- **Alt Negative Button**: `left`

#### Vertical（垂直移动）
- **Positive Button**: `w` 或 `up arrow`
- **Negative Button**: `s` 或 `down arrow`

#### Jump（跳跃）
- **Positive Button**: `space`

#### Fire1（攻击）
- **Positive Button**: `left ctrl` 或鼠标左键

#### Dash（冲刺）
- **Positive Button**: `left shift`

#### Interact（交互）
- **Positive Button**: `e`

#### Menu（菜单）
- **Positive Button**: `escape`

---

## 6. 测试场景

### 6.1 运行场景
1. 点击 Unity 编辑器顶部的 **Play** 按钮（▶️）
2. 或者按 `Ctrl + P`

### 6.2 测试功能
- **移动**：按 `A/D` 或 `←/→` 键，玩家应该左右移动
- **跳跃**：按 `Space` 键，玩家应该跳跃
- **查看控制台**：按 `F2` 打开控制台，查看调试信息

### 6.3 查看管理器状态
在 Game 视图中运行时，检查 Console 窗口：
- 应该看到 "GameManager Initialized" 消息
- 应该看到其他管理器的初始化信息

---

## 7. 进阶：添加调试工具

### 7.1 添加 DebugHelper
1. 在 Hierarchy 中右键点击 `Managers` → `Create Empty`
2. 命名为 `DebugHelper`
3. 添加 `DebugHelper` 脚本组件
4. 在 Inspector 中配置：
   - **Show FPS**: ✅
   - **Show Log Messages**: ✅
   - **Enable Cheats**: ✅

### 7.2 调试快捷键
运行场景后：
- **F1**: 显示/隐藏 FPS
- **F2**: 显示/隐藏日志窗口
- **F3**: 清空日志
- **F4**: 切换时间缩放（慢动作效果）

---

## 8. 场景结构示例

你的 Hierarchy 应该类似这样：

```
Scene
├── Main Camera
├── Managers
│   ├── GameManager
│   ├── EventManager
│   ├── InputManager
│   ├── TimeManager
│   ├── SceneManager
│   ├── SaveManager
│   ├── PoolManager
│   └── DebugHelper
├── Player
└── Ground
```

---

## 9. 常见问题

### Q: 管理器没有自动创建？
**A**: 管理器使用 Singleton 模式，会在第一次访问时自动创建。如果需要在 Hierarchy 中看到，可以手动添加。

### Q: 输入不响应？
**A**: 检查：
1. Input Manager 中的输入轴是否正确配置
2. PlayerController 脚本是否正确添加到 Player 对象
3. 是否在 Play 模式下测试

### Q: 玩家掉出场景？
**A**: 添加边界或更大的地面，或者添加边界碰撞体。

### Q: 如何保存场景？
**A**: 按 `Ctrl + S` 或点击 `File` → `Save`，确保场景已保存到 Assets 文件夹中。

---

## 10. 下一步

现在你已经有了一个基础场景，可以继续：
1. 添加更多游戏对象（敌人、道具等）
2. 创建 UI 界面
3. 添加音效和背景音乐
4. 实现游戏机制（生命值、得分等）
5. 创建更多场景并实现场景切换

---

## 📝 提示

- **保存场景**：经常保存场景（`Ctrl + S`）
- **使用版本控制**：建议使用 Git 管理项目
- **组织 Hierarchy**：使用空对象作为父对象来组织场景结构
- **命名规范**：使用清晰的命名，例如 `Player`、`Enemy_01`、`Ground_Left` 等

祝你开发顺利！🎮

